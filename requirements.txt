streamlit>=1.32.0
# Local LLM via Ollama -- no API key, no per-token cost. Also requires the
# Ollama app/server installed separately (https://ollama.com/download) and a
# model pulled (e.g. `ollama pull llama3.2`).
llama-index-llms-ollama>=0.10.0
llama-index-core>=0.14.0
llama-index-vector-stores-faiss>=0.6.0
llama-index-embeddings-huggingface>=0.7.0
sentence-transformers>=2.6.0
faiss-cpu>=1.8.0
pdfplumber>=0.11.0
pypdf>=4.0.0
# OCR fallback for scanned or broken-font PDFs. Also requires the Tesseract
# binary installed system-wide (Windows: winget install UB-Mannheim.TesseractOCR)
pypdfium2>=4.30.0
pytesseract>=0.3.10
numpy>=1.26.0
python-dotenv>=1.0.0
